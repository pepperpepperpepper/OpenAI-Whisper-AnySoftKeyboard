package com.anysoftkeyboard.demo;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Demo UI Theme receiver that demonstrates UI card functionality
 */
public class DemoUIThemeReceiver extends BroadcastReceiver {
    
    @Override
    public void onReceive(Context context, Intent intent) {
        // This will be called when the theme is loaded
        // For now, just a simple implementation
    }
}